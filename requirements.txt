# Web server
flask>=3.0

# Document / text extraction
python-docx>=1.1
pdfplumber>=0.11
trafilatura>=2.0

# OCR fallback for scanned PDFs (also requires the system tesseract-ocr and
# poppler binaries — not installable via pip; see README for per-OS commands)
pytesseract>=0.3
pdf2image>=1.17

# LLM clients
openai>=1.0.0
ollama>=0.4.0

# Local embeddings for hybrid BM25+embedding retrieval (ONNX runtime, no torch)
fastembed>=0.8

# Config
python-dotenv>=1.0.0

# C++ extension build (nciipc-prep/)
pybind11>=2.12
